info:
  name: Get active drivers(dropdown)
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:8000/api/drivers/active
  auth:
    type: bearer
    token: "{{auth_secret_08wg}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

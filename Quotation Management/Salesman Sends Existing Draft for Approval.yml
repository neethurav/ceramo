info:
  name: Salesman Sends Existing Draft for Approval
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8000/api/quotations/2/send
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_secret_0mb8}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

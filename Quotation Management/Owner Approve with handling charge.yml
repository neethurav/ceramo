info:
  name: Owner Approve with handling charge
  type: http
  seq: 9

http:
  method: POST
  url: http://127.0.0.1:8000/api/quotations/2/approve
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{\r

      \    \"handling_charge\": 3000.00,\r

      \    \"approval_notes\": \"Approved with 5% handling charge\"\r

      }"
  auth:
    type: bearer
    token: "{{auth_secret_08wg}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

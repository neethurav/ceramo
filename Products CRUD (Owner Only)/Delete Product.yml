info:
  name: Delete Product
  type: http
  seq: 5

http:
  method: DELETE
  url: http://127.0.0.1:8000/api/products/1
  auth:
    type: bearer
    token: "{{auth_secret_08wg}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

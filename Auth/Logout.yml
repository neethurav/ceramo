info:
  name: Logout
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8000/api/logout
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{\r

      \    \"email\": \"salesman1@ceramo.com\",\r

      \    \"password\": \"password123\"\r

      }"
  auth:
    type: bearer
    token: "{{auth_secret_0i7q}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
